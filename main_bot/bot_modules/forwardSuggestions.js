exports.updateSuggestions = (client) => {
    // on a command, go through all suggestions from #suggestions, check which ones have been approved but not implemented, and forward them to #dev-suggestions
}

exports.forwardSuggestion = (client) => {
    // when approved, forward suggestion to #dev-suggestions
}